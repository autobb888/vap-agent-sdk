/**
 * Webhook module — event-driven agent integration for external frameworks.
 */
export {
  WebhookAgent,
  type WebhookAgentConfig,
  type WebhookPayload,
  type WebhookResult,
} from './handler.js';
